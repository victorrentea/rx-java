package victor.training.rx.sample.madalina;

import java.util.Optional;
import java.util.UUID;

public class GetDocumentChunk {
   public Optional<DocumentChunk> execute(String fileName, UUID version, Integer integer) {
      return Optional.empty();
   }
}
